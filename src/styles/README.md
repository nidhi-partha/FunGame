Empty on purpose. Global styles live in `src/app/globals.css`.
Only add files here if we need styles that don't fit component-level Tailwind
classes or globals.css (e.g. a large set of hand-written keyframe animations).
